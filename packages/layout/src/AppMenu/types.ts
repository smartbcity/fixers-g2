export const classes = `interface AppMenuClasses {
  menu?: string
  logo?: string
}`
export const styles = `interface AppMenuStyles {
  menu?: MenuStyles
  logo?: React.CSSProperties
}`
export const StyleProps = `interface StyleProps {
  detailDrawerWidth: number;
  appBarHeight: number;
  menuWidth: number;
}`
